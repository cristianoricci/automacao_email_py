# Automação de Relatórios com PyAutoGUI 🚀

Projeto desenvolvido em Python para automatizar o processo de geração e envio de relatórios através da interface gráfica (RPA).

## 🛠️ Tecnologias e Bibliotecas
* **Python 3**
* **PyAutoGUI:** Para automação de cliques e teclado.
* **Pyperclip:** Para lidar com caracteres especiais e acentuação no Linux.
* **Time:** Para controle de pausas entre comandos.

## 🐧 Configuração para Linux (Debian)
Para que o PyAutoGUI funcione corretamente no Debian, é necessário instalar as seguintes dependências de sistema:
```bash
sudo apt update
sudo apt install python3-tk python3-dev scrot
```

## 🚀 Como executar
1. Clone o repositório.
2. Crie um ambiente virtual: `python3 -m venv venv`.
3. Ative a venv: `source venv/bin/activate`.
4. Instale os requisitos: `pip install pyautogui pyperclip`.
5. Execute o script: `python3 codigo.py`.

---
*Desenvolvido por [Cristiano Ricci](https://github.com/cristianoricci)*
